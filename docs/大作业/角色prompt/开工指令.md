# 开工指令（六个角色）· v2 —— 2026-09-17 22:45 修订

> **用途**：**约束提示词已经贴过、角色已就位**——每个角色复制自己那一段，贴进对应会话即可开工。
>
> **和「创建/约束提示词」的区别**：那些讲*边界*（你是谁、能碰什么）；**本文件讲*现在干什么***。
>
> **权威来源**：职责边界 = `AGENTS.md` **0.5 节**；任务归口 = **5.1 节**；卡 = `docs/大作业/任务卡/`。
>
> **⚠️ v2 为什么改**：21:03 的晚间验收快照判「T2 零产出、不能按期交付」——**那个判断在 21:40 复检时已被推翻**：
> T2 在 21:03 之后完成了。**下面这份表是 21:40 实测复核后的真实状态。**

---

## 0. 当前真实状态（2026-09-17 22:40 实测复核，非台账转抄）

| 卡 | 角色 | 状态 | 实测证据 |
| :-: | --- | :-: | --- |
| **T0** 建表脚本 | 代码负责 | ✅ | `laboratory_schema.sql` 5 条 `create table if not exists`；种子数据并入 |
| **T1** 单测 7→29 | 测试负责 | ✅ | 实得 **30** |
| **T2** 集成 0→7 | 测试负责 | ✅ | 实得 **7**；全量 `Tests run: 37, Failures: 0, Errors: 0` / `BUILD SUCCESS`（**PM 22:40 独立复跑确认**，1m45s） |
| **T6** 前端 UI 统一 | UI设计 | ✅ | 6 条 grep 全绿 + `vite build` EXIT=0；另**产出 4 张答辩素材截图** |
| **T3** CI 流水线 | 代码负责 | ⬜ | `.github/` 下**无 `workflows/`**（只有 `modernize/` 工具产物） |
| **T4** Dockerfile | 代码负责 | ⬜ | 全仓 `**/Dockerfile*` **零命中** |
| **T5** 设计模式 | 代码负责 | ⬜ | `laboratory` 包内**零显式工厂类** |
| **D1** 五份文档 | 文档负责 | 🔄 | **2 / 5 份 Markdown 初稿**（01 需求 319 行、02 架构 639 行）；**全仓 0 份 PDF** |
| **D2** 答辩 PPT | PPT和设计 | 🔄 | 14 页大纲 + 视觉规范 + **3 页定稿**；**0 份 pptx** |
| **D3** 源码 ZIP | 代码负责 | ⬜ | 仓库根无 `.zip`（9-20 执行） |
| **D5** 海报 | PPT和设计 | ⚠️ | 等阿辉（Q-07），建议先不做 |

**瓶颈已转移**：不再是「测试负责」，而是——

> ### 🔴 唯一瓶颈 = **代码负责**：T3 / T4 / T5 三张卡全压在一个会话上
> 关键路径 `T0 → T1 → ~~T2~~ → **T3** → D1 → D2 → D3`，**T0/T1/T2 已全部推过，现在就堵在 T3**。
> 且 **T5 卡着架构文档单项最高的 8 分**、**T3 卡着《CI/CD 文档》10 分 + 答辩硬性要求的流水线截图**。
>
> ### 🔴 最大失分风险 = **D1 的「0 份 PDF」**（占 70 分）
> 不是"第 3 份还没写"，是**PDF 化这条路一次都没走通**。

---

## 1. 开工前，六个角色都要知道的五件事

| # | 事项 | 口径 |
| :-: | --- | --- |
| **1** | **时间** | 今天 **9-17 = D-5 收盘**。**代码冻结 9-19（剩 2 天）**、打包 9-20（剩 3 天）、**上传超星 9-21（剩 4 天）**、**答辩 9-22（剩 5 天）** |
| **2** | **推送凭据** | 远程仓库已推过（`main = 57c81b9`）。⚠️ **Token 已吊销 → 任何角色都不许 `git push`**；本地 `commit` 后交阿辉/PM 用新凭据推。**当前本地领先远端 2 个 commit（`ed971fd`、`1668e4a`）+ 约 15 个未提交改动**（已登记 Q-16） |
| **3** | **收件箱协议** | 不卡当前的 → 往 `docs/大作业/待阿辉确认.md` **追加一行**（事项 / 选项 / **你的建议** / 卡住了谁）；当场不答就没法继续的 → **会话里直接问**。**没建议的不许进表** |
| **4** | **共享基建目录（新增，已收编）** | `.workbuddy/{tools,memory,logs}` **任何角色可写**，不算越界——**但只能放工具/记忆/日志，不得放业务产出**。`logs/pm-watch/` 只有 PM 写 |
| **5** | **会自动巡检** | PM 每天 **09:00 / 21:00** 跑真实命令验收（见 `AGENTS.md` 6.1 节）。**台账标 ✅ 却跑不出证据的卡会被点破**；越界会被 `git diff --name-only` 抓出来 |

**统一开头（每段指令的第一句）**：
> 先读 `AGENTS.md` 全文 + **第 5.1 节自己那一行** + 自己的任务卡，**再核一遍现状**（重读要改的文件 + `git status`），然后动手。

**统一结尾（每段指令的最后一句）**：
> 每完成一张卡：**跑验收命令 → 把真实输出贴回来 → 回写台账状态 → 写 AI 留痕**。不许跳过验收，不许编数字。

---

## 2. 代码负责 —— 贴在**已有的「代码负责」会话** 🔴 **最高优先级**

```text
【开工指令 · 代码负责 · 第二轮】（T0 已完成，进入 T3 / T5 / T4 / D-21）

先读 AGENTS.md + 第 5.1 节「代码负责」那一行 + T3/T4/T5 卡，再核一遍现状，然后动手。

【你现在是三张卡全压一个人，而关键路径全堵在你这里】
T0 已✅、T1✅、T2✅ 都推过去了，关键路径 `T0→T1→T2→【T3】→D1→D2→D3` 现在只堵在 T3。
按这个顺序做，别并行摊开：
  ① T3 CI 流水线 + 金丝雀   —— P1，【今天/明天上午先落 yml】，卡《CI/CD 文档》10 分 + 答辩硬性要求的流水线截图
  ② T5 设计模式（简单工厂）  —— P1，卡架构文档【单项最高的 8 分】，别拖到最后一天
  ③ T4 容器化 Dockerfile    —— P2，可与 T5 一起起手（后端单 jar + 前端静态产物，两段式）
  ④ D-21（见下）             —— 小活但不做答辩会翻车
  ⑤ D3 源码 ZIP             —— 9-20 打包日执行，要等 5 份 PDF 齐

【T3 不必等 T2 —— 已经解耦了】
T2 已完成（7 个集成测试、全量 37 全绿），T3 的前置彻底没有借口了。
CI 先落地 `.github/workflows/*.yml`，建议两条 job：`mvn -o test` + `vite build`；【金丝雀部署后补】。

【⚠️ 顺手把 D-21 做掉（5 分钟，但不做答辩开场就崩）】
真实库缺 `laboratory:dashboard:view` 菜单 → 实测 `select * from sys_menu where locate('dashboard',perms)>0` = 0 行，
后果是【除超管外任何账号登录首页都看不到运维看板】，老师看到的开场画面就是「当前角色暂没有运维工作台查看权限」。
修法：按文件头 [N/7] 顺序在真实库执行 `sql/laboratory_permission_fix.sql`（幂等，连跑两次无副作用）。

【硬边界】
可写：RuoYi-Vue-fast/src/main/**、RuoYi-Vue3/src/**、sql/、.github/、deploy/
不写：src/test/**（归测试负责）；不碰 docs/大作业/文档/**（归文档负责）
★ T6 已由 UI设计 收口，前端 src/ 现在别动，动了截图就作废（截图是答辩 PPT 的素材）。

【验收命令（逐条真跑，把输出贴回来）】
· bash .workbuddy/tools/mvnx.sh -o -B clean test → Tests run 不下降（当前基线 37 全绿 / Failures: 0 / Errors: 0）
· cd RuoYi-Vue3 后 node node_modules/vite/bin/vite.js build --mode production → 成功
  （★ 必须在 RuoYi-Vue3/ 目录内跑；若被安全删除护栏拦下 SAFE_DELETE_BULK_CONFIRM_REQUIRED，加 --emptyOutDir false）
· git ls-files | grep -c node_modules → 0
· T3/T4 各自的卡内验收项逐条勾
★ 本机 mvn 命令是坏的，一律用 .workbuddy/tools/ 下的包装脚本；离线必须加 -o。
★ 并发跑 Maven 会撞 target/ 目录，若报莫名编译错，等 2 分钟重跑。

【推送】Token 已吊销，本地 commit 就好，【不要 push】；写完汇报，由阿辉/PM 用新凭据推。

【留痕】台账改 🔄 再动手 → 每完成一张卡跑验收 → 回写第 6 节台账 + 第 7 节偏移（D-04 等）+ 写 AI留痕。
```

---

## 3. 测试负责 —— 贴在**已有的「测试负责」会话**

```text
【开工指令 · 测试负责 · 第二轮】（T1 / T2 已完成，进入「交件 + 加固」）

先读 AGENTS.md + 第 5.1 节「测试负责」那一行 + T1/T2 卡，再核一遍现状。

【你的两张卡都完成了，PM 已独立复核】
· T1：实得 30 个单测（LabRoleUtilsTest 5 / LabAsset 8 / LabRepair 12 / LabRoom 5）
· T2：实得 7 个集成测试（LabAssetIntegrationTest 2 + LabRepairIntegrationTest 5）
· 全量 `Tests run: 37, Failures: 0, Errors: 0` / `BUILD SUCCESS`（PM 22:40 复跑确认，1m45s）
  —— 21:03 那份巡检快照判你「零产出」，那是快照取早了，已在 22:40 复检中推翻，台账已改 ✅。

【本阶段第一件事：交件（别等文档负责来问）】
「文档负责」的《测试文档》整篇要靠你的数据，请整理成一份素材包交出去：
  ① 单元测试 N / 集成测试 N 的【真实】数字（跑出来的）
  ② 覆盖率证据（跑出来的，不是估的）
  ③ 测试运行的命令行 + **截图**（任务书硬性要求"测试运行截图"）
  ④ 每个用例覆盖了哪条业务规则（BR-01~BR-06 的对应关系）—— 文档里要画这张映射表
建议落到 `docs/大作业/AI留痕/T2-2026-09-17.md` 末尾或单独一份 `测试素材交付.md`，然后在收件箱知会一声。

【本阶段第二件事：两处已知遗留（择一或全做，都不许动 src/main）】
  · **D-07 遗留**：原 5 个状态机用例仍用 ReflectionTestUtils 打私有方法。可另写一组
    「经 `updateLabRepair` 公开入口」的状态机用例，把覆盖从"打私有"升级为"走真实入口"。
  · **D-19**：看板查询用了 H2 不支持的 `date_format()`/`date_sub()`，【已记录容忍】。
    ★ **绝不许改生产 mapper XML 去迁就 H2**（T2 卡第 4 节明令）。要补只能在测试侧注册同名自定义函数。
    记得在素材包里【如实写明"看板未覆盖"】——答辩若被问，含糊比承认更扣分。

【硬边界】
可写：RuoYi-Vue-fast/src/test/**
不改：src/main/**（发现生产缺陷 → 登记偏移，交代码负责修）
不碰：docs/大作业/文档/**（正文归文档负责，你只交【测试素材 + 截图】）

【验收命令】
bash .workbuddy/tools/mvnx.sh -o -B clean test → Tests run ≥37 且 Failures: 0
★ H2 必须写死 1.4.199（本机离线只有这版，Boot 2.5.15 默认指向 1.4.200 会拉不到）。
★ 并发跑 Maven 会撞 target/，报莫名编译错就等 2 分钟重跑。

【留痕】回写台账 + 写 AI留痕（D1 的测试文档就靠它）。
```

---

## 4. 文档负责 —— 贴在**已有的「文档负责」会话** 🔴 **最大失分风险**

```text
【开工指令 · 文档负责 · 第二轮】（2/5 初稿已成，进入「先打通 PDF」）

先读 AGENTS.md + 第 5.1 节「文档负责」那一行 + D1 卡，再核一遍现状。

【★ 本阶段第一件事，优先级高于继续写：把 PDF 化这条路走通】
现状：`文档/01-需求规格说明书.md`(319 行) 与 `文档/02-软件架构设计文档.md`(639 行) 两份初稿已在，
但【全仓 0 份 PDF】。**真正的风险不是"第 3 份还没写"，是 PDF 导出这条路一次都没验证过。**
→ 立刻拿 `01` 导一次 PDF、数页数（目标 8–12 页），把「md → PDF」的流程、页数区间、
  封面 `[待填]` 的排版效果一次性试出来。**这一步不打通，9-20 打包日会直接爆。**
→ 导出方式自选（Word/LaTeX/其它都行），但要在留痕里写清用的是哪条路、命令是什么，好复用。

【打通之后，继续按已批准的写作顺序推进】
② 架构（已初稿，需补表结构章节）→ ⑤ AI 报告 → ③ 测试文档 → ④ CI/CD
素材地图（这是你最需要盯的）：
  · ② 架构 —— 表结构章节【T0 已完成，可以写了】：`sql/laboratory_schema.sql` 5 张表的真实字段现成可抄；
    设计模式章节【等 T5，代码负责正在做】，没到位就先留骨架 + 标 [待填]。
  · ③ 测试 —— 数字/截图【T1/T2 已完成】，直接找「测试负责」要素材包（他会主动交）。
  · ④ CI/CD —— 【等 T3/T4】。AGENTS.md 第 8 节原本指向的 `deploy/local-pipeline.ps1` 【并不存在】（D-16），
    别去找它；等 T3 落地后用真实的 `.github/workflows/*.yml` 写。
  · ⑤ AI 报告 —— 留痕现成，但【第 1 节的工具清单卡在阿辉的 Q-06】，其余章节可以先写。

【保底顺序（阿辉已批：时间不够时按这个砍，宁可写薄也不许编数据）】
1 需求 → 2 架构 → 5 AI 报告 → 3 测试 → 4 CI/CD

【封面与署名 —— 唯一来源，直接照抄】
`docs/大作业/封面署名信息.md`：第 6 组 / 王旻辉、巴力江·托力肯别克、廖煌、迪丽热巴·阿布都西克尔 /
软件工程 2023-2 / 学号 2311231056（只给了这一个）/ 授课教师 谭清萱 / 答辩日 2026-09-22。
【仍留空】编写人、审核人、文档日期、二级学院、其余 3 人学号 —— 没确认的一律 [待填]，不许编。

【硬边界】
可写：docs/大作业/文档/**、AI留痕/D1-*.md
不碰：任何代码；不动 AGENTS.md 第 6/7 节台账。
★ 每份开头必须有「AI 使用声明」；查重 >30% 直接 0 分 —— 初稿写完必须人工改写再定稿。

【验收】五份页数命中各自区间 / 每份有 AI 使用声明 / 逐份导出 PDF 并核对页数。
```

---

## 5. UI设计 —— 贴在**已有的「UI设计」会话**

```text
【开工指令 · UI设计 · 第二轮】（T6 已完成，进入「交件 + 冻结前保障」）

先读 AGENTS.md + 第 5.1 节「UI设计」那一行 + T6 卡，再核一遍现状。

【T6 已完成，PM 复核通过】
6 条验收 grep 全绿 + vite build EXIT=0；第 10.3 节 4 项目视不确定项已在真实环境逐项量过
（V-1 固定列不破 / V-2 折叠态四角 8px / V-3 1920 无横滚 / V-4 三按钮 186px < 220px 单行不换行）；
连带修了 D-22（资产操作列 260→320）。**干得干净。**

【本阶段第一件事：交件】
`AI留痕/截图/T6-核心功能界面/` 里那 **4 张答辩素材截图**（首页看板 / 资产台账 / 资产履历时间线 / 报修处理时间线）
是「PPT和设计」D2 卡第 3 类页面的素材 —— **主动告诉他截图已就绪、路径在哪**，别等他来找。
★ 这 4 张是 9-19 冻结前采的，**冻结后 UI 再改就作废**。

【本阶段第二件事：演示路径巡检（冻结前，9-19 之前做完）】
按答辩现场实际要演示的顺序，在真实环境走一遍 4 条路径，确认每一步都能点通、不出 403：
  ① 登录（⚠️ 注意 D-20：脚本实际口令是 `admin123`，不是文档写的 `123456` —— 阿辉正在拍板改哪边）
  ② 首页看板（⚠️ 注意 D-21：真实库缺 dashboard 菜单，非超管会看不到；已交代码负责修）
  ③ 资产台账 → 扫码/履历
  ④ 报修提交 → 审核 → 处理 → 评价（完整闭环）
发现问题**只登记 + 汇报**，不要顺手改代码（T6 已收口，冻结前不动前端）。

【硬边界】
可写：RuoYi-Vue3/src/views/laboratory/**、assets/styles/**（截图与巡检记录另放 AI留痕/）
不改：首页 views/index.vue 视觉基线
★ 9-19 冻结后不再改前端。**不要新建 laboratory.scss**（样式基线已存在，另起文件会造成两套令牌打架）。

【验收】T6 卡第 6 节 6 条 grep 保持全绿；vite build 保持 EXIT=0（改前改后各跑一次）。
```

---

## 6. PPT和设计 —— 贴在**已有的「PPT和设计」会话**

```text
【开工指令 · PPT和设计 · 第二轮】（3 页定稿，进入「批量填充」）

先读 AGENTS.md + 第 5.1 节「PPT和设计」那一行 + D2 卡，再继续推进。

【进度】14 页大纲 + 视觉规范 + 封面/第 3 页架构/第 4 页状态机【已定稿】；其余 8 页素材来源已定，只等填。
**现在 0 份 pptx —— 页数是硬指标（≤15），尽早出实体文件。**

【本阶段第一件事：先填【与代码状态无关】的页，别等 T0~T4 全收口】
可以先做的：背景/需求、AI 协作方式、项目定位、状态机与业务规则（第 4 页已定稿，可复用其语法）。
必须等的：
  · 第 7 页部署 / 第 8 页 CI —— 等 T3/T4（代码负责正在做）
  · 第 9 页测试数据 —— 【T1/T2 已完成，可以填了】：全量 37 个用例（单测 30 + 集成 7）、0 失败。
    素材找「测试负责」，他会主动交一份素材包（含数字、覆盖率、运行截图）。
  · 第 11 页偏差 / 第 13 页 —— 等 T3/T4/T5 收口后再定。
★ 第 3 类页面「核心功能界面」的截图【已就绪】：`AI留痕/截图/T6-核心功能界面/`（4 张，UI设计已采）。
  但注意：截图是在 D-21 未修的状态下用 admin 取景的，**答辩演示时若换成 labadmin 取景，画面会不同**。
除了等不到真数据的页，**一律标 [待填]，不许留假数、不许编**。

【已定稿的三页请保持不动】尤其第 4 页状态机：**同色不同形**（待维修 = 蓝描边、维修中 = 蓝实心），
语义分组「描边＝等待或终止 / 实心＝进行中或已完成」——这是全场最值钱的一页，别为单个状态新调色相。

【硬边界】
可写：docs/大作业/答辩PPT/**、AI留痕/D2-*.md
不碰：任何源码；色板必须与 laboratory.scss 同源（每个色值都要能指到出处）。

【验收】
· 总页数 ≤15（当前预算 14）
· 每个色值都能在 laboratory.scss 找到出处——找不到就是新调的，必须改回来
· 字体用本机真实可用的（Noto Sans SC / Sarasa Gothic SC / Source Han Sans CN）；
  【本机没有 Microsoft YaHei】，写了会静默回退、版式整体错位
· 注释字号下限 22px

【D5 海报】仍在等阿辉定用途（收件箱 Q-07），【先不做】。
```

---

## 7. 产品经理 —— **就是本会话，不用张贴**

| # | 本轮 PM 要盯的 |
| :-: | --- |
| 1 | 🔴 **盯 T3**——关键路径唯一堵点，且压在代码负责一个人身上（同时还有 T4/T5/D-21）。明天（9-18）若 T3 没落地，9-19 冻结日必失守 |
| 2 | 🔴 **盯 D1 的 PDF 通路**——0 份 PDF 是 70 分的最大风险，比"第 3 份还没写"严重得多 |
| 3 | ✅ 已做：**补录 D-18**（21:03 承诺的欠账）、**5.1 节收编 `.workbuddy/{tools,memory,logs}`**、**Q-12 销项** |
| 4 | 催阿辉：**Q-15（D-20 演示口令，答辩现场会翻车）**、**Q-16（安排一次 push）**、Q-06（AI 工具清单） |
| 5 | 巡检台账回写——今晚已抓到 **T6 达标仍 🔄、D1 已开工仍 ⬜**（= D-18），明天继续盯 |

---

## 附：一页速查

| 角色 | 台账状态 | 接下来做什么 | 卡在谁身上 |
| --- | :-: | --- | --- |
| **代码负责** | T0✅ **T3/T4/T5 ⬜** | **T3 先落 yml** → T5 工厂（8 分）→ T4 → **D-21 跑 permission_fix** → D3(9-20) | **无（唯一瓶颈）** |
| **测试负责** | T1✅ T2✅ | **交件**（数字+覆盖率+截图）+ 处理 D-07 遗留 / D-19 | 无 |
| **文档负责** | 🔄 2/5、**0 PDF** | **先拿 01 导一次 PDF 打通流程** → 再继续写 | 第 ④ 份等 T3/T4 |
| **UI设计** | T6✅ | **交件**（4 张截图给 PPT）+ 冻结前演示路径巡检 | 无 |
| **PPT和设计** | 🔄 3 页定稿 | 先填与代码无关的页；第 9 页测试数据可以填了 | 第 7/8/11/13 页等 T3/T4/T5 |
| **产品经理** | — | 盯 T3 + 盯 PDF + 催阿辉 | — |
