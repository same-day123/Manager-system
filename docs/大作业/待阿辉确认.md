# 待阿辉确认（统一决策清单）

> **这张表就是"需要你拍板、但不必当场回答"事项的唯一收件箱。**
> 六个并行会话各自的待确认项，**全部汇总到这里**，你一次看完、一次回复，
> 不用在「产品经理 / 代码负责 / 测试负责 / 文档负责 / UI设计 / PPT和设计」之间来回找。
>
> **维护人**：产品经理（PM）。**最后更新**：2026-09-18 16:5x（D-4 下午巡检后清理）
> **本轮销项**：Q-06 / Q-08 / Q-17 / Q-18 / Q-19 / Q-11 **全部落地**（其中 Q-17/18/19 = D-24/D-25 演示数据校准，已于 9-18 00:0x 经阿辉授权在真实库执行完毕）。
> **阿辉 9-18 已拍板**：**Q-16**（给新 Token，PM 代推）、**Q-07**（D5 先不做）、**D2 测试数字**（用 **49**）。
> **仍待拍板（只剩 1 条）**：**Q-15**（演示口令 admin123 vs 文档 123456，阿辉问"这是什么的密码"——已在下方 Q-15 行补全解释）。
> **权威关系**：本表只管"谁在等什么决定"；流程与边界仍以 `AGENTS.md`（0.5 节职责 / 5.1 节归口）为准。

---

## 1. 三条使用规则

| # | 规则 |
| :-: | --- |
| **R1** | **什么进这张表**：影响后续工作、但**不卡住当前这一步**的决策。 |
| **R2** | **什么不进这张表**：**对话中需要马上确认才能继续的**（如"这个方案选 A 还是 B"）——**就在该会话里当场问**，不往这里堆。 |
| **R3** | **每条必须有「建议」**——只抛问题不给建议的条目等于把活推回给阿辉，PM 巡检时会打回。 |

**各角色怎么登记**：在自己的那一行**追加**（`提出方` 写自己的角色名），**不改别人的行**；
PM 巡检时统一排优先级、补建议、编号。**已回复的改成 ✅ 并写清结论与生效日期，不删历史。**

**阿辉怎么回复**：直接在对话里按编号说即可（例：`Q-02 选 B`、`Q-05 做，用答辩现场`），PM 负责销项与转达。

---

## 2. 🔴 高优先级（卡住他人开工 / 关键路径）

| # | 提出方 | 事项 | 结论 / 选项 | 卡住了谁 | 状态 |
| :-: | --- | --- | --- | --- | :-: |
| **Q-16** | 产品经理 | **需要一次 push**——本地领先远端 **8 个 commit**（`ed971fd` T0 / `1668e4a` D-17 / `1e0acb7` T3 / `b887a9c` T5 / `b219679` T4 / D-24/D-25 校准…）+ **约 60 个未提交文件**（T1/T2/T6 测试与 UI、文档初稿、截图、PDF…） | **建议尽快安排（这是当前最大单点风险）**。距 9-20 打包日只剩 2 天——**机器一坏，T1~T5 全部产出只存在于本机工作区**。Token 已吊销、无人 push，纪律执行得很干净；需你用新凭据推，或授权我代推 | 交付物 7 安全；T3 流水线前置 | ⏳ 待你安排 |
| **Q-15** | 产品经理 | **D-20 演示账号口令与全部文档不符**：脚本实际写入的是 **`admin123`**，而 `README.md` / `AGENTS.md` / 脚本注释三处都写「密码统一 `123456`」 | **✅ 已销项（2026-09-18 阿辉定「走 A」）**：三处文本统一改为 `admin123`——`laboratory_user_seed.sql` 注释 + `AGENTS.md` 第 1 节 + `README.md`。**不改脚本哈希、不改真实库**（零风险）。D-20 已在 7.3 标 ✅ | 答辩演示；复现性 | ✅ 已销项 |

| **Q-17** | UI设计 | **D-24 首页看板数字自相矛盾**：指标卡显示「**维修中资产 5**」与「**待审核报修 0**」并置。真实库有 5 台 `status='2'` 资产，但其中 3 台**没有任何工单** | **✅ 已销项（2026-09-18，并入 Q-19 / D-24）**：真实库已执行 `[5/7]` + `laboratory_demo_refresh.sql`，维修中 **5→2**、在途工单 **0→2**（1 待审核 + 1 维修中，可现场演示闭环）。**连带副作用：T6-01 看板截图过期，须重拍** | 答辩开场画面；交付物 7 | ✅ 已销项 |
| **Q-18** | UI设计 | **D-25 首页看板两张图为空**：「近七日报修趋势」「近七日维修费用」是 0 线（0 单 / ¥0.00）。根因：看板 SQL 只看**近 7 天**，而演示工单日期全是 2026-07-02/03 → 全被滤掉 | **✅ 已销项（2026-09-18，并入 Q-19 / D-25）**：真实库已执行 `laboratory_demo_refresh.sql` 把演示单日期平移回近 7 天，趋势图 **5 天有柱子（09-16 含 ¥120 成本）**。答辩前 1 天再跑一次校准即可保持饱满 | 答辩开场画面观感 | ✅ 已销项 |
| **Q-19**<br>*（暂拟编号，PM 可改）* | **代码负责** | **Q-17 / Q-18（= D-24 / D-25）的修复脚本已写好、并在影子库双路径验证通过；问：是否**现在就改真实库**？** 之所以不擅自执行，是因为改完真实库会让 **T6-01「首页运维看板」截图变成"说谎的证据"**——它现在拍到的正是「维修中 5 / 两张空图」，必须请「前端UI负责」重拍一次，**这是一次跨角色返工，需要你点头**。顺带汇报两处**根因修正**：① 真实库是 **5 台**维修中资产**全部**没工单（不止 3 台）；② D-25 不是"种子写死日期"，而是旧行只插不更新、**日期会自然老化**。 | **✅ 已销项（2026-09-18 00:0x 阿辉授权执行）**：按顺序跑 `[5/7] laboratory_user_seed.sql` + `laboratory_demo_refresh.sql`（两条幂等），先 `mysqldump` 备份。结果：维修中 **5→2**、在途工单 **0→2**、待审核报修 **1**、近七日趋势 **5 天有柱子（09-16 含 ¥120）**、断言 **12/12 全绿**、资产规模 **32 台不变**。**连带副作用：T6-01 看板截图过期，须重拍**（交件台第 1 行） | 答辩开场画面；T6 截图是否返工 | ✅ 已销项 |
| **Q-14** | 产品经理 | **⚠️ 安全：你在对话里贴出的 GitHub Token 需要吊销** | **✅ 已处置**（阿辉 2026-09-17 已吊销）。该 Token 只内联在命令里用过、**从未写进任何文件**。**后续约定：任何角色都不 `git push`**（本地 commit 后交阿辉/PM 用新凭据推） | 你的账号安全 | ✅ 已处置 |
| **Q-13** | 产品经理 | 5 天冲刺压缩倒排与保底顺序 | **✅ 走方案**：D-5~D 日逐日口径（`AGENTS.md` 3.1 节）；文档优先保「1 需求 / 2 架构 / 5 AI 报告」，`3 测试`、`4 CI/CD` 时间不够就写薄、不许编数据 | 全员排期 | ✅ 已确认 |
| **Q-04** | 产品经理 | 首次 `commit` + `push` 的提交范围 | **✅ 已完成**：不需你手动操作，PM 已代做——修复嵌套仓库（gitlink）问题后 `--amend` 得 **699 文件 / 82212 行**，`push` 成功，远端 `main = 57c81b9`（`ls-remote` 核验）。VSCode 方式说明保留在下方备份 | 代码负责（T3 靠推送触发 CI） | ✅ 已完成 |

### Q-04 已执行记录（含"用 VSCode 怎么做"的备份说明）

**结论（已落地）：不需要你手动 commit——PM 已代做并推送成功。**
首次 `push` 踩到一个坑：`RuoYi-Vue-fast/`、`RuoYi-Vue3/` 目录里各自带着**从上游 clone 来的内层 `.git`**，git 会把这两个目录整体当成 **gitlink（mode 160000）** 跳过 → 首次提交只进了 38 个文件、**源码全没进去**。
修复：把两个内层 `.git` **移动**到 `D:\code\_Manager_system_git_backup\`（备份可逆、没删），再 `--amend` → **699 文件 / 82212 insertions**（含 `RuoYi-Vue-fast/src/...` Java 源码），推送后远端 `refs/heads/main` = `57c81b9`，与本地一致。

**已核查过的提交范围**（`.gitignore` 实际生效）：

| | 内容 |
| --- | --- |
| ✅ **会提交** | `.gitignore`、`AGENTS.md`、`README.md`、`RuoYi-Vue-fast/`、`RuoYi-Vue3/`、`docs/`、`.workbuddy/{memory,tools}/` |
| ❌ **已排除** | `RuoYi-Vue3/node_modules`、`RuoYi-Vue3/dist`、`RuoYi-Vue-fast/target`、`.workbuddy/logs/` |
| ⚠️ **凭据** | `push` 到 GitHub 需要凭据 —— 你已提供（用户名 + Token），**本次已用掉**。请按 **Q-14** 把它吊销，下次续推再给新的 |

**备份：如果你想自己在 VSCode 里重新做一次（或以后自己推）**
1. `Ctrl + Shift + G` 打开 **Source Control（源代码管理）**面板
2. 在 "Changes" 右侧点 **`+`（Stage All Changes）**→ 全部进入 "Staged Changes"
3. 上方输入框写提交信息，例如 `chore: 初始化仓库，纳入源码与项目管理文档` → 点 **✓ Commit**
4. 点 **Sync Changes / Push** → 首次会弹 GitHub 登录授权，按提示登录

> ⚠️ **注意**：若你在 VSCode 里看到 `RuoYi-Vue3/node_modules` 出现在待提交列表 —— **先停下告诉我**，
> 那说明 `.gitignore` 没生效，直接提交会把仓库撑到几个 G、`push` 必失败。
> 正常情况它**不会**出现（已实测被 `RuoYi-Vue3/.gitignore` 挡住）。

---

## 3. 🟡 中优先级（影响产出质量，不卡开工）

| # | 提出方 | 事项 | 选项 | 建议 | 卡住了谁 | 状态 |
| :-: | --- | --- | --- | --- | --- | :-: |
| **Q-06** | 文档负责 | **《AI 辅助开发实践报告》的工具说明**——实际用过哪些 AI 工具 | **✅ 已销项（2026-09-17 第 14 轮已给全）**：DeepSeek V4 Flash / GLM 5.3 Flash / VS Code / WorkBuddy。第 5 份文档已据此写第 1 节 | 交付物 5 | ✅ 已销项 |
| **Q-07** | PPT和设计 | **D5 海报做不做、做哪几张、用在哪** | 选题展示／答辩现场／项目宣传／不做 | **先不做**，等 T0~T5 收口；任务书 7 项交付物里没有海报，属加分尝试 | PPT和设计（D5，⚠️ 阻塞中） | ⏳ 待答 |
| **Q-08** | 产品经理 | **第 5 节人类组员姓名**（需求/架构文档、测试文档、CI-CD与AI报告、答辩与打包 各自谁认领） | **✅ 已销项（2026-09-17）**：4 位成员姓名 + 班级 + 授课教师已随封面署名信息补齐（王旻辉 / 巴力江·托力肯别克 / 廖煌 / 迪丽热巴·阿布都西克尔 / 软件工程 2023-2 / 谭清萱）。**各文档的具体认领人仍留空待补**（此项不影响文档产出，9-20 打包前补齐即可） | `AGENTS.md` 第 5 节完整性 | ✅ 已销项 |
| **Q-09** | 产品经理 | **提示词第 ⑧ 段的节奏**：新角色「先汇报再动手」还是「直接干、边干边报」 | 二选一 | **保留"先汇报再动手"**（方向错了返工更贵） | 五份角色提示词的统一口径 | ⏳ 待答 |
| **Q-10** | 产品经理 | 首页权限门控（修 D-10 时加的 `views/index.vue`）**是否补一张 T7 卡正式收口** | 补卡／归编码职责内即可 | **补卡**——它的性质是功能性修复，但踩在 T6 卡"不改首页视觉基线"的边界词上，补卡可消除台账歧义 | 台账一致性 | ⏳ 待答 |

---

## 4. 🔵 低优先级 / 只需知会

| # | 提出方 | 事项 | 建议 | 状态 |
| :-: | --- | --- | --- | :-: |
| **Q-11** | 文档负责 | `AGENTS.md` 第 8 节把 `deploy/local-pipeline.ps1` 写成流水线截图来源，但**该文件尚不存在** | **✅ 已销项（2026-09-17 第 18 轮 T3 完成）**：`deploy/local-pipeline.ps1` 已真实存在并跑通（`PASS 4 / SKIPPED 2 / FAIL 0`），第 8 节「流水线运行截图」现在有真实落点 | ✅ |
| **Q-12** | 文档负责 | `README.md`「已知待办」仍写**"未初始化 Git 仓库"**，与 D-09 已解除（**且已完成首次推送**）矛盾（**C4 检查不通过**） | **✅ 已销项**：随提交 `1668e4a` 修完（README 该行已改写为「已解除」，并补上 `57c81b9` 已推送、`ed971fd` 待推的事实），**7.1 节 C4 检查恢复通过** | ✅ |
| **Q-17**<br>*（测试负责占号，与 §2 的 Q-17 撞号）* | **测试负责** | **测试素材包已交件 → 文档负责**（阿辉开工指令第一件事）。测试侧从 37 个用例加固到 **49 个**（单测 30→**33**、集成 7→**16**），`Tests run: 49, Failures: 0, Errors: 0` / `BUILD SUCCESS` 独立复跑两次一致。主文档 `AI留痕/测试素材交付.md` 含：真实数字 / 运行命令与环境三前提 / **BR-01~06 全量追溯矩阵（58 条映射）+ US-13 看板** / 等价类与边界值 / 截图清单 | **✅ 已交件（纯知会，不需拍板）**。请文档负责答辩口径对齐两点：① **行/分支覆盖率百分比出不来**（离线缺 JaCoCo）→ 文档只写"用例↔BR 矩阵 + 断言 177 / 交互校验 27"，**不编百分比**；② **D-19 已收口**——看板**已覆盖**（8 条 mapper 查询、7 个用例），答辩被问应回答"测了，不改生产代码、测试侧补同名函数" | 文档负责（第 3 份《测试文档》素材） | ✅ 已交件 |

---

## 5. ✅ 已确认（保留结论，不删）

| # | 事项 | 结论 | 确认日期 |
| :-: | --- | --- | :-: |
| — | 选题方向 | 走 **选题 D 自拟**（不是选题 B 预约体系） | 2026-09-16 |
| — | 编码负责人归属 | **AI（Claw）** | 2026-09-17 |
| — | 答辩材料与视觉设计负责人 | **AI（Claw）**（职责四） | 2026-09-17 |
| — | 远程仓库 | `https://github.com/same-day123/Manager-system.git` | 2026-09-17 |
| — | 角色扩编 | 新增 **⑤ 文档负责**、**⑥ 测试负责**；T1/T2 从「代码负责」划出 | 2026-09-17 |
| — | 本会话角色 | 产品经理 | 2026-09-17 |
| — | 任务归口形式 | 在 `AGENTS.md` 加**角色索引**（不另开清单文件） | 2026-09-17 |
| **Q-01** | 答辩日期与提交截止 | **终期答辩 2026-09-22**；**最终提交 2026-09-21**（答辩前 24 小时）；其余里程碑仍 `[待填]`（已写入 `AGENTS.md` 第 3 节 + 3.1 压缩倒排） | 2026-09-17 |
| **Q-02** | **D-15：T0 新脚本以谁为基准** | **走 B**——按 T0 卡写（普通索引）+ 幂等迁移修正真实库的两个唯一键；`decimal` 精度与 `room_id` 可空性两条**以真实库为准**（唯一索引与逻辑删除 `del_flag` 语义冲突，属真缺陷） | 2026-09-17 |
| **Q-03** | **D-13 / D-14：种子数据缺口** | **并入 T0** 一并补（`lab_room` 5 条 / `lab_repair` 4 条 + `lab_repair_record` 1 条） | 2026-09-17 |
| **Q-05** | **封面与署名信息** | 已给全——**唯一来源 = `docs/大作业/封面署名信息.md`**：第 6 组／成员 4 人／软件工程 2023-2／学号 2311231056／授课教师 谭清萱；**编写人、审核人、文档日期、二级学院、其余 3 人学号 留空待补** | 2026-09-17 |
| **Q-13** | 5 天冲刺压缩倒排与保底顺序 | **走方案**：D-5~D 日逐日口径；文档优先保「1 需求 / 2 架构 / 5 AI 报告」，`3 测试`、`4 CI/CD` 时间不够就写薄、不许编数据 | 2026-09-17 |
| **Q-04** | 首次 `commit` + `push` | **PM 已代做并成功**：修复 gitlink（嵌套仓库）→ `--amend` 699 文件 → `push origin main`；远端 `main = 57c81b9`（`ls-remote` 核验）。**D-09 彻底收口，T3 前置就绪** | 2026-09-17 |
| — | 上传超星 + 彩排 由谁做 | **阿辉本人做**；AI 侧只需把代码/文档改到能过阿辉检验 | 2026-09-17 |
| **Q-14** | GitHub Token 安全处置 | **阿辉已吊销**。后续约定：**任何角色都不 `git push`**，本地 commit 后交阿辉/PM 用新凭据推 | 2026-09-17 |
| **Q-16** | 安排一次 push | **✅ 阿辉 2026-09-18 拍板：提供新 Token，PM 代推**（积压 8 commit + 约 60 文件）。待阿辉给出新 PAT 后执行 | 2026-09-18 |
| **Q-07** | D5 海报 | **✅ 阿辉 2026-09-18 拍板：先不做**（集中精力收口文档 + PPT + ZIP 三件硬交付物） | 2026-09-18 |
| — | D2 测试数字口径 | **✅ 阿辉 2026-09-18 拍板：用 49**（单测 33 + 集成 16，与磁盘 @Test 计数、两次实跑一致） | 2026-09-18 |

---

## 6. ⏸️ 已决定不做 / 挂起（保留原因）

| # | 事项 | 决定 | 日期 |
| :-: | --- | --- | :-: |
| **G-2** | 答辩内容 / 现场演示是否另开 AI 角色 | **暂不处理**（任务书要求全员参与陈述、教师随机点名，AI 代不了；留到答辩准备期由人类组员认领） | 2026-09-17 |

---

*本表的价值在于"一次问清、一次答完"。**没有建议的条目不许进表**——那是把判断推回给阿辉。*
