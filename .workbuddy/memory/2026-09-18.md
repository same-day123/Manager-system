# 2026-09-18

- PM 早间布防巡检（实际 16:41~16:50，自动化 c67764e2）：快照 `.workbuddy/logs/pm-watch/2026-09-18-1650.md`。49 用例独立实跑全绿；瓶颈从代码侧转到文档侧（D1 剩第 3/4 份）。关键提醒：Q-16 push 积压已滚大到 8 commit + 约 60 文件；T6-01 截图须 9-19 冻结前重拍（D-24/25 校准后旧图过期）；5.1 边界表 3 处文字缺口拟登记 D-29（pom.xml / 任务卡回写 / AI留痕目录）；收件箱 Q-06/08/17/18/19 共 6 条过期 ⏳ 待清理；D2 台账"第 8 页受 [4/6] FAIL 阻塞"备注已过期（T3 实测 PASS）。
- 环境坑复现：后台 Bash 的 PATH 全坏（bash 自身 127）→ Maven 验收必须走 PowerShell + `mvnx.ps1`，输出 `2>&1 | Out-File -Encoding utf8` 落盘再 Read。

## 测试负责 · 第三轮（18:0x，冻结前保障基线复跑）

- 承接阿辉「开工指令 · 测试负责 · 第三轮」（T1/T2 全完成 + 交件完成，进入「冻结前保障 + 待命」）。
- **核现状**：5.1 测试负责行 / T1(→33)/T2(→16) / D-07(✅已修)/D-19(✅已收口) 台账全部一致；交付物 7 个文件全部在位；`src/test` 无未提交改动；PM 今日已补「AI留痕/ 写权限约定」（测试负责写 `T1-*`/`T2-*`，正好合规）。
- **基线复跑（关键动作）**：因昨日 T5 重构动了 `src/main`（`LabRecordFactory`/`LabAssetEvent`/`LabRepairEvent` 落盘），在**今天的代码上**亲自复跑全量 `clean test` → **`Tests run: 49, Failures: 0, Errors: 0, Skipped: 0` / BUILD SUCCESS（02:13，Finished 18:07:48）**。逐类分布与上轮一致（2+7+7 集成 / 8+12+3+5+5 单测）。日志 `.workbuddy/logs/t2c-baseline-0918.log`。
- WMIC 黑名单拦截再次出现在**外层 PowerShell 包装**上（`PROGRAM BLOCKED: WMIC.exe`），但子进程已跑完、日志完整落盘（85809 字节），与测试结果无关——仍是我们定位过的 `PpidChecker` 噪声，只是这次沙箱把「拦截」显式报成了 PROGRAM BLOCKED。
- **结论**：`Tests run: 49` 不被打破 ✅。进入待命，随问随答；不主动动 `src/main`。

## PM 处置（16:5x，D-4 下午，承接早间巡检快照的「PM 自己要做的事」）

阿辉转发了 16:50 巡检简报，问怎么推进。当前会话 = **Claw（PM）**，按快照 §八.5「PM 自己（普通会话）」清单逐项落实：

1. **补 5.1 边界表 3 处文字缺口**（已收编，非行为越界）：
   - 测试负责可写目录补 `RuoYi-Vue-fast/pom.xml`（仅限 test-scope 依赖）+ `AI留痕/{T1,T2}-*` + `测试素材交付.md`
   - UI设计补 `AI留痕/T6-*` + `AI留痕/截图/**`
   - 5.1 下方新增两条通用约定：各角色可写**自己卡号开头**的 `AI留痕/` 文件（不改别人卡号）；执行人可在**自己认领的任务卡末尾**追加验收结果小节（不改主体）
2. **登记 D-29 进 7.3**（当前最大编号，接 D-28）——性质标「边界表文字缺口、非越界行为」，已收编。
3. **清理收件箱 5 条过期项**：Q-06（AI 工具清单已给全，DeepSeek V4 Flash / GLM 5.3 Flash / VS Code / WorkBuddy）、Q-08（组员姓名已随封面署名补齐）、Q-17/Q-18/Q-19（= D-24/D-25 演示数据校准，9-18 00:0x 经阿辉授权已在真实库执行完毕）。另有 Q-11 也随 T3 完成销项。**撞号说明**：§2 的 Q-17（UI设计 D-24）与 §4 的 Q-17（测试负责素材交件）撞号，后者已标「测试负责占号」区分。
4. **更正 D2 台账过期备注**：第 8 页「受 T3 流水线 [4/6] 打包产物 FAIL 阻塞」→ 改为「已消除（实测 PASS 4 / SKIPPED 2 / FAIL 0）」。
5. **刷新 Q-16 积压数字**：2 commit + 15 文件 → **8 commit + 约 60 文件**（当前最大单点风险，距打包日 2 天）。

### 收件箱清理后剩余待阿辉拍板（只剩 3 条）
- 🔴 **Q-15**：演示口令 admin123 vs 文档 123456（建议改文档，走 A）
- 🔴 **Q-16**：安排 push（积压 8 commit + 60 文件，机器一坏全没）
- 🟡 **Q-07**：D5 海报（建议不做）
- 另：D2 台账「测试数字 37 还是 49」待裁定（PM 建议 49，磁盘 @Test 与两次实跑均为 49）

## Q-15 走 A 已落地 + 新 Token 推送被「workflow 权限」卡住（17:0x~17:1x）

阿辉拍板：**Q-15 走 A**（文档改 admin123），并提供**新 Token**（`repo` 权限，已吊销，见下文）。

### Q-15 落地（D-20 销项）
改了 3 处演示账号密码文本 `123456` → `admin123`：
1. `RuoYi-Vue-fast/sql/laboratory_user_seed.sql` 第 9 行注释 + 第 16~21 行账号清单
2. `AGENTS.md` 第 1 节演示账号表
3. `README.md` 第 231 行
**注意**：`ry_20260417.sql:563` 的 `sys.user.initPassword='123456'` 是**另一个概念**（若依「新建用户初始密码」配置项），与演示账号无关，**保持不动**（已在 D-20 行注明）。
D-20 已在 7.3 标 ✅，Q-15 已销项。

### push 卡住：token 缺 workflow 权限
- 本地 `git add -A` + `git commit` 成功：**`ec9337d`**，103 文件 +11909/−61（含 D-20 文本 + 5.1 边界收编 + 收件箱清理 + T1/T2/T2b/T6 留痕 + 3 份 PDF + 大量截图/工具脚本）
- 推送**被远端拒绝**：
  `! [remote rejected] main -> main (refusing to allow a Personal Access Token to create or update workflow .github/workflows/ci.yml without workflow scope)`
- 根因：新 Token **只勾了 `repo`，没勾 `workflow`**。推送里含 `.github/workflows/ci.yml`（T3 产物），GitHub 强制要求 workflow 文件的写权限。
- **待办**：阿辉需在 GitHub 重新签发 Token，**勾上 `workflow`**（在 repo 基础上多勾一个），再给我新的即可继续推。本地 `ec9337d` 已就绪、工作区干净，**只差推这一步**。

### 教训
- GitHub fine-grained/classic PAT 推送含 `.github/workflows/` 时必须带 `workflow` scope；只勾 `repo` 会在 push 阶段（不是 commit 阶段）被拒。这是本项目的 T3 产物带来的新约束，之前首次 push 时还没 ci.yml 所以没触发。

## 阿辉三项拍板 + Q-15 待解释（16:5x~17:0x）

用 AskUserQuestion 一次问了 4 件，阿辉答了 3 件、对 Q-15 反问"这是什么的密码"：

| 项 | 阿辉答复 | 落账 |
| --- | --- | --- |
| Q-16 push | **给新 Token，PM 代推** | ✅ 待阿辉给新 PAT 后执行 |
| Q-07 海报 | **先不做** | ✅ 已销项 |
| D2 测试数字 | **用 49** | ✅ 已定 |
| Q-15 口令 | 反问"什么的密码" | ⏳ 待解释后拍板 |

### Q-15 根因钉死（回答阿辉的疑问）
- 这是**演示账号登录密码**（labadmin/asset01/repair01/room01/student1/viewer01 6 个）。
- `laboratory_user_seed.sql:23` 的哈希 `$2a$10$7JB720yubVSZvUI0rEqK/...` **直接复用了若依原生 admin 的哈希**（`ry_20260417.sql:83` 也是它）→ 明文是 **`admin123`**，不是文档写的 `123456`。
- 文档三处（脚本注释 / README / AGENTS 第 1 节）写「密码统一 123456」→ 与事实不符。
- 实测：`labadmin`+`123456` → 500 失败；`labadmin`+`admin123` → 成功。
- 已在收件箱 Q-15 行补了白话解释 + A/B 两方案，等阿辉选。

### 教训
- **Q-15 这类"技术黑话"要先翻译成用户能懂的话再问**——我第一轮 AskUserQuestion 直接抛"演示口令 admin123 vs 文档 123456"，阿辉没懂。正确姿势是先说清楚"这是演示账号登录密码，答辩现场要用它登录系统"。

## push 收口成功（17:2x）——Q-16 销项

阿辉第二次签发带 `workflow` 权限的 Token（已吊销，见下文），PM 推送成功：

- `57c81b9..ec9337d main -> main`，`PUSH_EXIT=0`（352 objects / 295 写入 / 4.84 MiB）
- 核验：`git fetch` 后 `UNPUSHED` 为空，本地与远端完全同步。
- **当前最大单点风险（机器一坏 T1~T5 全没）已解除。** Q-16 已在收件箱销项。

### 教训
- 推送日志里的 `RemoteException` / `CategoryInfo: NotSpecified` 是 PowerShell 把 git 的 stderr 进度输出（`Enumerating/Counting/Writing objects`）当成错误流，**只要看到 `PUSH_EXIT=0` 且最后一行是 `<old>..<new> main -> main` 就是真成功**，别被红色报错误导。
- 验证推送是否彻底同步，**别用 `origin/main..HEAD`**（用临时 token URL push 不会更新本地 `origin/main` 引用，会报 `unknown revision`）——正确做法是 `git fetch <token-url> main` 后再 `git log FETCH_HEAD..HEAD` 看是否为空。
- **⚠️ 严重教训：绝不要把明文 PAT 写进任何会被 `git add -A` 收录的文件**（本文件就是教训——把两个真实 Token 记进了记忆，触发 GitHub push protection 拦截）。Token 只允许内联在命令行 `$url` 变量里、用完即弃。写记忆/日志时一律用「（已吊销）」代指。

## ⚠️ 安全事件：两个 Token 明文泄入本地 git 历史（17:2x）

- 起因：我把阿辉给的两个真实 PAT 明文写进了本记忆文件，随 `634621f` 提交。
- 后果：`git push` 被 GitHub push protection 拦截（`GH013: Push cannot contain secrets`，定位到 `634621f` 的 `.workbuddy/memory/2026-09-18.md` 第 27、70 行）。
- 处置：① 已从记忆文件删掉两个明文 Token；② 已用 `git reset --soft ec9337d` 撤销含 Token 的 `634621f`，重新提交为 `d1350d0`（干净版），`git log -p` 扫描历史 `ghp_` 匹配 **0 处**，随后推送成功 `ec9337d..d1350d0`。③ **仍需阿辉吊销这两个 Token**（`repo` 权限那个、`workflow` 权限那个）——虽已从 git 历史移除、远端也已干净，但 Token 明文曾短暂存在，且仍在你和我之间的对话记录里出现过，**吊销最稳妥**。

## 重发开工指令 v3（18:0x，阿辉完成拍板后安排各角色下一步）

阿辉完成四项拍板（Q-15 走 A / Q-16 push / Q-07 海报不做 / D2 数字 49）后，要求给其他角色安排下一步。我核对了 AGENTS.md 第 6 节台账 + 7.3 偏移 + 16:50 巡检快照，确认最新状态后，把 `docs/大作业/角色prompt/开工指令.md` 从 v2（9-17 22:45，已严重滞后）重写为 **v3**（9-18 18:00）。

### v3 的核心变化（vs v2）
1. **代码负责**：v2 判「T3/T4/T5 三卡全压、唯一瓶颈」→ **已全部完成**，现只剩 D3 打包（9-20），今天只做打包准备。
2. **测试负责**：v2 还在催交件 → **已交件完成**，现「待命，随问随答」。
3. **文档负责**：v2 还在「打通 PDF」→ **PDF 已打通、3/5 出稿**，现唯一瓶颈是第 3《测试》/ 第 4《CI/CD》未动笔（素材已齐 20 小时）。
4. **UI设计**：v2 还在交件 → 新增**冻结前最急**事项：重拍 T6-01（D-24/25 校准后旧图是说谎证据，明晚 9-19 冻结）。
5. **PPT和设计**：v2 的第 8 页 FAIL 阻塞、第 9 页 37/49 争议 → 均已消除（T3 PASS + 数字定 49），恢复推进。
6. 阿辉四项拍板全部写入各角色指令（口令 admin123 / 已 push / 海报不做 / 数字 49）。

### 现状锚点（9-18 18:00）
- 关键路径 T0→T1→T2→T3 **全线 ✅**；D1 3/5 份 PDF；D2 9/14 页；D3 ⬜（9-20）。
- 两个 🔴：① 文档负责的第 3/4 份 PDF（唯一瓶颈）；② UI设计重拍 T6-01（冻结前最急）。

## 文档负责 · 第三轮（18:0x，D1 五份交付物全部出稿）

承接阿辉「开工指令 · 文档负责 · 第三轮」（3/5 出稿，冲刺最后两份）。

- **核现状**：5.1 文档负责行 / D1 卡 / 验收清单页数区间（03=6–10、04=5–8，与 `build.mjs` RANGE 一致）全部对上；两份素材（`测试素材交付.md`、T3/T4 留痕 + `ci.yml`/`docker-compose.yml`/README）均已齐。
- **03《测试计划与用例文档》**：`文档/03-测试计划与用例文档.md` + `pdf/03-...pdf`，**7 页 PASS**（区间 6–10），一次成型未压页。数字全取自素材包（49=33+16、断言 177、verify 27、BR 矩阵 58 条）。两条如实说明写进正文第 9 章。2 张测试截图留 `[待填]` 占位（PNG 需人工截取）。
- **04《CI/CD 部署方案》**：`文档/04-CI_CD部署方案.md` + `pdf/04-...pdf`，**5 页 PASS**（区间 5–8），一次成型。三环境表 + 四 job 流水线（引用 ci.yml 不重贴）+ 金丝雀排除法理由 + Dockerfile 加分项。两条红线遵守（未等 `docker compose ps` 截图，§6.2 已验证/未验证分开；未照抄 T4 卡错误示例，§6.3 单列两处缺口）。
- **全量 `node build.mjs` 复跑：五份 PDF 全 PASS**（01=8 / 02=13 / 03=7 / 04=5 / 05=6），字体嵌入 + 文字可复制均 ✅。
- **回写**：`AI留痕/D1-2026-09-17.md` 追加第 15（第 3 份）、16（第 4 份）节；`AGENTS.md` 第 5 节台账追加一行（未动第 6/7 节）。
- **MEMORY.md 精简**：原 44 行超 4000 字符限制，重写去重（移除与 AGENTS.md 头部纪要重复的逐轮细节，保留代码约定 + 状态锚点 + PDF 流水线 + 压页经验 + 两条如实说明口径）。

### 统一定稿清单（五份共用，等阿辉 + 人工）
1. 补封面 `[待填]`（二级学院/专业、日期、编写人/审核人、其余成员学号）。
2. 人工改写表述防查重（红线 1；正文「防查重提示」块删除）。
3. 03 补 2 张测试运行截图 PNG。
4. 三件事齐后统一重新导出五份 PDF。


---

## UI设计 第三轮（18:05-18:2x）：T6-01 重拍完成

**唯一任务**：D-24/D-25 数据校准后重拍首页看板截图（9-19 冻结前）。

- **环境隔夜全灭**（5173/8080/9222 均 False）→ 重起：jar 缓存（D:\code\_Manager_system_build_cache\ruoyi.jar，JDK 17 jdk-17.0.19 + --server.port=8080，Bash run_in_background 方式拉起成功；PowerShell Start-Process 沙箱下不落 PID）+ dev 5173 + Chrome 9222（eshoot_t6_01.mjs 内 ensureChrome detached）。
- **截图完成**：T6-01-首页运维看板.png 2026-09-18 18:12:44 / 109.5 KB（旧图同路名覆盖作废）。画面与校准后真实库逐一核对一致：**32 / 2 / 1 / 3** + 趋势两图 **5 天有柱**（09-16 ¥120）；dmin/admin123 取景（Q-15 走 A，不再用 123456）。
- **⭐ 新发现：看板趋势图坐标轴随系统日期滚动**（新图右端 09-18）——「近 7 天」窗口是动态的，9-19 冻结当天画面依然真实，无需再拍；若答辩前又跑了 laboratory_demo_refresh.sql，同天跑 .workbuddy/tools/reshoot_t6_01.mjs 一键刷新。
- 其余 3 张（T6-02/03/04）不受影响，沿用 9-17 版。
- **文档同步**：AGENTS.md 头部第 22 轮 + 6.2 交件台追加「18:1x UI设计」行（取代 9-17 行空图警告）+ D-24/D-25 行注记已重拍；T6 留痕新增 9.1.1 重拍记录节。
- **验收**：git diff 前端两目录零改动；6 条 grep 全绿（1535px 不变）；vite build 前台直跑 + --emptyOutDir false → **EXITCODE=0**（r6）。
- 环境进程留存（8080/5173/9222），PPT和设计补截图可复用。

## 代码负责 · 第三轮（18:0x，D3 打包脚本与清单备好，9-20 出包）

- 承接阿辉「开工指令 · 代码负责 · 第三轮」：四张卡（T0/T3/T4/T5）全完成、全量 49 绿，本阶段只剩 D3 源码 ZIP；**今天不是打包日，只备脚本 + 清单**。
- **核现状（关键发现）**：
  1. **D3 任务卡其实不存在**（任务卡目录只有 D1/D2/T0~T6）→ 按 AGENTS §5.1/§6 的 D3 行执行。
  2. **5 份 PDF 已实测齐全**（不是开工指令说的 3/5）：03 测试、04 CI/CD 也已出。03/04 的 `.pdf` 与 `.md` 源**尚未 git commit**（`??` 状态，文档负责的活跃产出）；01/02/05 已跟踪但工作区是定稿后新版。
  3. 04 的真实文件名是 `04-CI_CD部署方案.pdf`（**下划线** `CI_CD`，不是连字符），与 md 源一致。
- **打包方案**：`.workbuddy/tools/d3_package.py` 用 `git archive --format=zip` 只导**已跟踪文件** → 天然排除 target/dist/node_modules/logs + 23 个巡检临时脚本（`patrol_*`/`ui_probe*`/`h2probe/` 等），**不用真删任何东西**（避开安全删除护栏风险）。5 份 PDF 单独**从工作区读定稿版**写入（跳过 git 里的同名条目），保证交付定稿而非 git 旧版。
- **命名**（唯一来源 `封面署名信息.md`）：`软件新技术专题-第6组-高校实验室资产与报修管理平台.zip`。
- **踩坑（已修，可复用）**：① Windows 下 `git ls-files` 输出含中文文件名时是 GBK，subprocess 不能 `encoding='utf-8'`，改 bytes + utf-8/gbk 回退解码；② `git archive -o <中文/临时路径>` 会 "could not open ... for writing"，改 **stdout 重定向收 bytes** 最稳；③ 不落临时 tar 树、直接用 zipfile 内存重写，跨平台一致。
- **验证**：dry-run（缺一份即阻断）+ 真实打包（临时造 04 冒烟 + 最终 5 份真实）均通过——798 文件 / 8.58 MB / PDF 恰好 5 份零重复 / target·dist·node_modules·临时脚本全排除。
- **回写**：AGENTS.md D3 行 ⬜→🟡（脚本+清单就绪）、代码负责进度行补 9-18 更新；产出 `d3_package.py` + `d3_package_清单.md`。
- **边界**：`src/main`/`src/test`/前端 `src/`/`docs/大作业/文档/**` 全零改动（只读 PDF）。
- **9-20 出包前必须做**：① 代码侧 8 个未推送 commit + 文档侧 03/04 PDF 都先 commit（`git archive` 取的是已提交版）；② 跑 `python .workbuddy/tools/d3_package.py` 出包到仓库根。

## D2 答辩 PPT · 第 4 轮（PPT和设计，2026-09-18 晚）

- **14/14 页全部出稿**：补 07 测试金字塔 / 08 交付链路 / 09 演示导引 / 11 数据模型 / 13 规模指标；12 页扩为五条容忍偏差（D-26/D-28 按指令加入）；14 页补六角色分工清单。测试数字按阿辉批示落 **49**（37/49 之争收口）。
- **实体文件**：`答辩PPT-v1.pptx`（14 页）+ `答辩PPT-v1.pdf`（14 页，逐页 PNG→Chrome CDP 合成；`export_nodes pdf` 会把多页拍平成 1 页，弃用）+ `导出预览/` 14 张 PNG。
- **★ 重大发现：Ardot pptx 导出缺陷**——导出器把每段文本首尾的 CJK/全角字符串剥掉（类 = U+3000-303F / U+4E00-9FFF / U+F900-FAFF / U+FF00-FFEF；`·`U+00B7、`①`U+2460 不剥；剥空则整段保留）。v1 实测 297 个 run 缺段、10/14 页受影响；画布与 PNG/PDF 不受影响。克隆实验证明与编辑历史无关，属管线固有缺陷。
- **修复**：`.workbuddy/tools/d2_pptx_repair.py` 以画布 batch_read 文本为真值逐 run 对齐回填（S10 一个双 run 节点走显式补丁）→ **`答辩PPT-v2.pptx` 逐页校验 0 缺失，交付以 v2 为准**。经验：导出成功 ≠ 文本完整，实体文件必须回读文本层；版式/页数验证以 PNG/PDF 为准。
- **回写**：AGENTS.md §6 D2 行（第 4 轮纪要 + 剩余两项外部输入）、slide-outline.md（素材表/进度表 14/14/口径收口）、留痕 `D2-2026-09-17.md` 第 23~28 节、MEMORY.md 清理瘦身（超限截断 → 收敛到 ~3000 字内）。
- **遗留**：第 9 页三个截图位待 T6-01 新图排入；分工认领 Q-08 待阿辉；D5 海报阿辉已定先不做。

## PM 复核 + 重发开工指令 v4（19:05，阿辉「又完成一轮，重新检查重新安排」）

阿辉再次要求复核后重排任务。我实测核对（19:03）：本轮各角色已把关键路径基本收口，项目从「冲刺」进入「收尾」。

### 实测确认的最新状态（vs v3 的 18:00）
| 项 | v3 时 | 现在 | 证据 |
| --- | --- | --- | --- |
| D1 文档 | 3/5 PDF | ✅ **5/5 全部出稿** | `文档/pdf/01~05.pdf` 全实存（03 测试 7 页 / 04 CI/CD 5 页 本轮新产出） |
| D2 PPT | 9/14 页设计稿、0 pptx | 🔄 **14/14 出稿 + 实体文件** | `答辩PPT-v2.pptx`(14页) + `答辩PPT-v1.pdf` + 14 预览图；修复 pptx 导出缺陷 |
| D3 ZIP | ⬜ | 🟡 **脚本备好** | `d3_package.py` 用 git archive，5 份 PDF 已实测齐全，9-20 一键出包 |
| T6-01 | 待重拍 | ✅ **已重拍** | `T6-01-首页运维看板.png` 18:12 更新（32/2/1/3 + 趋势 5 天有柱） |

### 收尾阶段的三件事（重排后的唯一待办）
1. 🔴 **git 积压又滚大**：本地领先远端 1 commit（`7cd81a1`）+ 约 40 未提交文件（5 份 PDF 定稿 + PPT 实体 + T6-01 新图）——当前唯一单点风险，需阿辉给新 Token 尽快 push。
2. 🟡 **D2 第 9 页截图替换**：T6-01 已重拍，PPT和设计 需替换进画布第 9 页 → 重导出 v2.pptx + 预览图。
3. 🟡 **封面署名空位回填**：编写人/审核人/日期/二级学院/其余 3 人学号（`封面署名信息.md` 第二节），文档 + PPT 封面共用，等阿辉提供。

### 我的动作
- 重写开工指令 v3 → **v4**（`角色prompt/开工指令.md`）：五个角色从「冲刺」改为「收尾」——代码负责=打包日待命、测试负责=完全待命、文档负责=封面回填+补验页数、UI设计=交件新 T6-01、PPT和设计=替换第9页截图重导出。
- 台账 D1 行 🔄 → ✅（5/5 PDF 出稿，收尾待办列明）。

### 教训
- **阿辉说「又完成一轮」时，各角色可能已在各自会话里干完并留痕**——复核要读记忆日志（`2026-09-18.md` 里已有多条角色轮次记录），别只信台账（台账状态列常滞后于实际，如 D1 行之前还停在 🔄 但 PDF 已 5/5）。
